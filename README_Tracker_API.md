# Yandex Tracker API v3 — Коллекция запросов

Коллекция HTTP-запросов для [Insomnia](https://insomnia.rest/), [Postman](https://www.postman.com/) и [Bruno](https://www.usebruno.com/) на основе официальной документации:  
**https://yandex.ru/support/tracker/ru/api-ref/about-api**

## Содержимое

152 запроса в 34 папках, покрывающих все разделы публичного API Яндекс Трекера v3.

| Папка | Запросов | Описание |
|---|---|---|
| Задачи | 11 | Создание, получение, редактирование, удаление, поиск, переходы |
| Задачи — связи | 3 | Связи между задачами |
| Задачи — внешние связи | 5 | Ссылки на задачи во внешних системах |
| Фильтры задач | 5 | CRUD для сохранённых фильтров |
| Чеклисты | 6 | Пункты чеклиста внутри задачи |
| Комментарии | 4 | Комментарии к задачам |
| Прикреплённые файлы | 5 | Загрузка и управление файлами задачи |
| Пакетные операции | 4 | Массовые изменения, переходы, перемещения |
| Поля задачи | 2 | Глобальные поля задач |
| Проекты, портфели, цели | 6 | Новый API сущностей (entityType) |
| Сущности — комментарии | 4 | Комментарии к проектам/портфелям/целям |
| Сущности — файлы | 3 | Файлы проектов/портфелей/целей |
| Сущности — связи | 3 | Связи проектов/портфелей/целей |
| Сущности — чеклист | 6 | Чеклисты проектов/портфелей/целей |
| Проекты (старая версия) | 6 | Устаревший API проектов `/v3/projects` |
| Очереди | 6 | Просмотр и удаление очередей |
| Очереди — версии | 4 | Версии внутри очереди |
| Очереди — компоненты | 5 | Компоненты очереди |
| Очереди — локальные поля | 5 | Локальные поля очереди |
| Автодействия | 5 | Автодействия очереди |
| Триггеры | 5 | Триггеры очереди |
| Макросы | 5 | Макросы очереди |
| Доски задач | 5 | CRUD для досок |
| Доски — колонки | 5 | Колонки на доске |
| Доски — спринты | 5 | Спринты на доске |
| Дашборды | 5 | CRUD для дашбордов |
| Компоненты | 5 | Глобальные компоненты |
| Типы задач | 2 | Справочник типов задач |
| Статусы | 2 | Справочник статусов |
| Резолюции | 2 | Справочник резолюций |
| Приоритеты | 2 | Справочник приоритетов |
| Учёт времени | 4 | Записи о затраченном времени |
| Импорт | 4 | Импорт задач, файлов, комментариев, времени |
| Пользователи | 3 | Информация о пользователях организации |

## Импорт в Insomnia

1. Открой Insomnia
2. **File → Import → From File**
3. Выбери `api-tracker-requests-library.yaml`
4. Создай окружение: **Environments → Manage Environments** и добавь переменные:

```json
{
  "token": "<твой OAuth-токен>",
  "org_id": "<ID организации>"
}
```

---

## Импорт в Postman

Для Postman используй отдельный файл в нативном формате:  
**`api-tracker-requests-library.postman_collection.json`**

1. Открой Postman
2. **Import → Upload Files** → выбери `api-tracker-requests-library.postman_collection.json`
3. Postman создаст коллекцию с 34 папками и 152 запросами
4. Настрой переменные: **Environments → Add**

```json
{
  "base_url": "https://api.tracker.yandex.net",
  "token": "<твой OAuth-токен>",
  "org_id": "<ID организации>"
}
```

5. Переменные `{{token}}` и `{{org_id}}` подтянутся автоматически во всех запросах

---

## Импорт в Bruno

Bruno поддерживает прямой импорт через формат **OpenCollection (YAML)**:

1. Открой Bruno
2. **Import Collection**
3. В поле **File Format** выбери `OpenCollection (YAML)`
4. Выбери файл `api-tracker-requests-library.yaml`
5. Нажми **Import**

**Переменные окружения в Bruno:**
1. Открой настройки коллекции → вкладка **Environments**
2. Нажми **+** и создай окружение (например, `Трекер`)
3. Добавь переменные:

| Name | Value |
|---|---|
| `token` | `<твой OAuth-токен>` |
| `org_id` | `<ID организации>` |

4. Нажми **Save** и выбери созданное окружение

---

## Переменные окружения

| Переменная | Заголовок | Где используется |
|---|---|---|
| `token` | `Authorization: OAuth {{token}}` | Все запросы |
| `org_id` | `X-Org-ID: {{org_id}}` | Организация Яндекс 360 для бизнеса |

> Если организация привязана к **Yandex Cloud**, замени заголовок `X-Org-ID` на `X-Cloud-Org-ID` вручную в нужных запросах.

## Получение OAuth-токена

1. Перейди на [https://oauth.yandex.ru](https://oauth.yandex.ru) и создай приложение, или используй существующий токен
2. Запроси доступ: **tracker:read** (чтение) и/или **tracker:write** (запись)
3. Подробнее: [Доступ к API](https://yandex.ru/support/tracker/ru/api-ref/access)

## Базовый URL

```
https://api.tracker.yandex.net/v3/
```

## Параметры в URL

Запросы содержат плейсхолдеры — замени их на реальные значения перед отправкой:

| Плейсхолдер | Пример значения |
|---|---|
| `{issueId}` | `TREK-123` |
| `{queueId}` | `TREK` |
| `{boardId}` | `42` |
| `{entityType}` | `project`, `portfolio`, `goal` |
| `{entityId}` | `63a5dc2b1*****` |
| `{commentId}` | `18` |
| `{sprintId}` | `7` |

## Структура запроса

Каждый запрос уже содержит:
- Заголовок `Authorization` с `{{token}}`
- Заголовок `X-Org-ID` с `{{org_id}}`
- Заголовок `Content-Type: application/json` (для POST/PATCH)
- Пример тела запроса в формате JSON (для POST/PATCH)
- Query-параметры с пагинацией (`perPage`, `page`) где применимо
